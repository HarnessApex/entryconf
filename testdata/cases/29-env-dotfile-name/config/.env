# SPEC §4: the *.env pattern matches every name ending in ".env", including
# the bare name ".env". If this file were skipped, the references below would
# fail with E_MISSING_VAR instead of resolving.
EC_DOTFILE_HOST=db.local
EC_DOTFILE_PORT=5432
