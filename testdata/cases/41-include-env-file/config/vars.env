# SPEC §5: *.env files cannot be included and ".env" is not a parser
# extension, so "@file:vars.env" is E_INCLUDE. This file is well formed as a
# variable file, so loading it as one is not a second fault.
EC_UNUSED=1
