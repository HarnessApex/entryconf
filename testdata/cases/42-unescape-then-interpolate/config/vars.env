# SPEC §5: a leading "@@" becomes a literal "@" and the string is never
# re-examined for "@file:" — but, like any other string, it is still
# interpolated (SPEC §6), so the unescaped result below carries EC_N's value.
EC_N=5
