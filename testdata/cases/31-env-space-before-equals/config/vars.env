# SPEC §4: lines are trimmed before classification, so indentation is fine,
# but whitespace between NAME and "=" is not. The entrypoint deliberately does
# not reference EC_X: *.env files are loaded unconditionally (SPEC §1 step 2),
# so the single fault is this malformed line.
EC_X = 1
